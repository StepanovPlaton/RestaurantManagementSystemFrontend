# Public API base (same-origin proxy in dev)
NEXT_PUBLIC_API_URL=/api

# Backend origin for Next.js rewrites (server-side only)
API_PROXY_TARGET=http://192.168.0.1:8080

# Optional: media files base (defaults to /uploads)
# NEXT_PUBLIC_MEDIA_URL=/uploads
